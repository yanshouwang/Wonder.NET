<Page
    x:Class="Wonder.UWP.Views.BLEDeviceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wonder.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <RelativePanel>
            <TextBlock x:Name="NameTBK" Text="{x:Bind ViewModel.Name}"
                       Style="{StaticResource HeaderTextBlockStyle}"
                       RelativePanel.AlignHorizontalCenterWithPanel="True"
                       RelativePanel.AlignVerticalCenterWithPanel="True"/>
            <Button Content="BACK" Style="{StaticResource ButtonRevealStyle}"
                    RelativePanel.AlignHorizontalCenterWithPanel="True"
                    RelativePanel.Below="NameTBK"
                    Command="{x:Bind ViewModel.GoBackCommand}"/>
        </RelativePanel>
    </Grid>
</Page>
