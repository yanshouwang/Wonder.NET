<UserControl
    x:Class="Wonder.UWP.Views.LEServiceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Wonder.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Wonder.UWP.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ComboBox x:Name="CharacteristicsCBX" Grid.Row="0"
                  ItemsSource="{x:Bind ViewModel.Characteristics}"
                  Header="选择特征值"
                  SelectionChanged="CharacteristicsCBX_SelectionChanged">
            <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="vm:LECharacteristicViewModel">
                    <TextBlock Text="{x:Bind UUID, Converter={StaticResource GUIDToStringConverter}}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <ContentControl x:Name="CharacteristicView" Grid.Row="1"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch">
            <local:LECharacteristicView/>
        </ContentControl>
    </Grid>
</UserControl>
